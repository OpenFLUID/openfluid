

FOREACH(THIRD_NAME ${OFBUILD_3RDPARTY_LIBS_LIST})
  SET(THIRD_DIR ${THIRD_NAME}-${OFBUILD_3RDPARTY_LIB_${THIRD_NAME}_VERSION})
  MESSAGE(STATUS "Added 3rdparty library: ${THIRD_NAME} (version ${OFBUILD_3RDPARTY_LIB_${THIRD_NAME}_VERSION})")
  ADD_SUBDIRECTORY(${THIRD_DIR})
ENDFOREACH()

