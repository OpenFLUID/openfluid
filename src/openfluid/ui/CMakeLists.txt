
FILE(READ "${PROJECT_SOURCE_DIR}/COPYING" UI_LICENSE_TEXT)
STRING(REGEX REPLACE "\n" "\\\\n" UI_LICENSE_TEXT "${UI_LICENSE_TEXT}")
STRING(REGEX REPLACE "\"" "''" UI_LICENSE_TEXT "${UI_LICENSE_TEXT}")

FILE(READ "${PROJECT_SOURCE_DIR}/AUTHORS" UI_AUTHORS_TEXT)
STRING(REGEX REPLACE "\n" "\\\\n" UI_AUTHORS_TEXT "${UI_AUTHORS_TEXT}")
STRING(REGEX REPLACE "\"" "''" UI_AUTHORS_TEXT "${UI_AUTHORS_TEXT}")


INCLUDE_DIRECTORIES(${QT_INCLUDES})


ADD_SUBDIRECTORY(common)
ADD_SUBDIRECTORY(waresdev)

IF(OPENFLUID_ENABLE_MARKET)
  ADD_SUBDIRECTORY(market)
ENDIF()  


CONFIGURE_FILE(config.hpp.in config.hpp @ONLY)

INSTALL(FILES "${CMAKE_CURRENT_BINARY_DIR}/config.hpp" 
        DESTINATION ${INCLUDE_OPENFLUID_INSTALL_PATH}/ui)