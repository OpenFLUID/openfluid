

FILE(GLOB TESTS_BUILDEREXTENSIONS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} tests.builderext.*)

FOREACH(TEST_BEXT ${TESTS_BUILDEREXTENSIONS})
  OFBUILD_ADD_BUILDER_EXTENSION(${TEST_BEXT} ${OFBUILD_TESTS_BINARY_DIR})
ENDFOREACH()

