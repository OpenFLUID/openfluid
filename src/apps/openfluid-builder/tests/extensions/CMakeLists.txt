
ADD_DEFINITIONS(-DOPENFLUID_VERSION=${FULL_VERSION})
ADD_DEFINITIONS(-DBUILDINGDLL)


FILE(GLOB TESTS_BUILDEREXTENSIONS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} tests.builder.*)

FOREACH(TEST_BEXT ${TESTS_BUILDEREXTENSIONS})
  ADD_BUILDER_EXTENSION(${TEST_BEXT} ${TEST_BEXT} ${TEST_OUTPUT_PATH})																						
ENDFOREACH(TEST_BEXT IN ${TESTS_BUILDEREXTENSIONS})

