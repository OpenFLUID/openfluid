

ADD_EXECUTABLE(${OPENFLUID_GITASKPASS_APP}
               main.cpp)

SET_SOURCE_FILES_PROPERTIES(main.cpp PROPERTIES COMPILE_DEFINITIONS "OFBUILD_GITASKPASS_ENVVAR=\"${OFBUILD_GITASKPASS_ENVVAR}\";OFBUILD_GITASKUSER_ENVVAR=\"${OFBUILD_GITASKUSER_ENVVAR}\"")

SET_TARGET_PROPERTIES(${OPENFLUID_GITASKPASS_APP} PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${OFBUILD_DIST_BIN_DIR}")


INSTALL(TARGETS openfluid-gitaskpass
        RUNTIME DESTINATION ${OFBUILD_BIN_INSTALL_PATH})


IF(OFBUILD_ENABLE_TESTING)
  ADD_SUBDIRECTORY(tests)
ENDIF()

