


ADD_SUBDIRECTORY(openfluid-minimal)

IF(BUILD_APP_MINIMALGUI)
 ADD_SUBDIRECTORY(openfluid-minimal-gui)
ENDIF(BUILD_APP_MINIMALGUI)

IF(BUILD_APP_ENGINE)
  ADD_SUBDIRECTORY(openfluid-engine)
ENDIF(BUILD_APP_ENGINE)

IF(BUILD_APP_BUILDER)
  ADD_SUBDIRECTORY(openfluid-builder)
ENDIF(BUILD_APP_BUILDER)

IF(BUILD_APP_MARKETCLIENT)
  ADD_SUBDIRECTORY(openfluid-market-client)
ENDIF(BUILD_APP_MARKETCLIENT)
