

IF(OFBUILD_TESTS_ENABLE_FORTRAN)

  SET(CMAKE_Fortran_FLAGS_RELEASE "-funroll-all-loops -fno-f2c -O3")

  ADD_LIBRARY(tests.fortran90${OPENFLUID_SIMULATORS_SUFFIX} MODULE Fortran90Sim.cpp SbrModFortran.f90)
  SET_TARGET_PROPERTIES(tests.fortran90${OPENFLUID_SIMULATORS_SUFFIX} 
                        PROPERTIES PREFIX "" SUFFIX "${OPENFLUID_PLUGINS_BINARY_EXTENSION}" 
                        LIBRARY_OUTPUT_DIRECTORY "${OFBUILD_TESTS_BINARY_DIR}")
  TARGET_LINK_LIBRARIES(tests.fortran90${OPENFLUID_SIMULATORS_SUFFIX} openfluid-base openfluid-core gfortran)

ENDIF()
