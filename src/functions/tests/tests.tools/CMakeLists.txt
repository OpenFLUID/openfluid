
INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS})

ADD_LIBRARY(tests.tools  MODULE ToolsFunc.cpp)

SET_TARGET_PROPERTIES(tests.tools 
                      PROPERTIES PREFIX "" SUFFIX "${FUNCTIONS_BINARY_EXTENSION}" 
                      LIBRARY_OUTPUT_DIRECTORY "${FUNCTIONS_OUTPUT_PATH}")

TARGET_LINK_LIBRARIES(tests.tools  ofelib ${Boost_SYSTEM_LIBRARY} ${Boost_FILESYSTEM_LIBRARY})
